{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the GuildBotics desktop window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    {
      "identifier": "shell:allow-execute",
      "allow": [
        {
          "name": "binaries/guildbotics-app-api",
          "cmd": "binaries/guildbotics-app-api",
          "sidecar": true,
          "args": true
        }
      ]
    }
  ]
}
