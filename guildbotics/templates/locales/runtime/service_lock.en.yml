en:
  already_running: "Another background service is already running."
  already_running_with_owner: "Another background service is already running (owner: %{owner}, PID: %{pid}, workspace: %{workspace})."
  desktop_managed: "The background service is managed by Desktop. Stop it from Desktop."
  invalid_metadata: "The background service lock is held, but its owner metadata is invalid."
  missing_process: "The background service lock is held, but process %{pid} is not running."
  not_running: "No background service is running."
  stopped: "Background service stopped."
  stopped_after_cancel: "Background service stopped after cancelling in-flight work."
  force_killed: "Force killed background service."
  stop_timeout: "Timeout reached; the background service may still be finishing in-flight work. Run `guildbotics stop` again to cancel it, or use --force to SIGKILL."
  permission_denied: "Permission denied to signal process %{pid}."
  process_missing: "Process %{pid} does not exist."
  sigkill_failed: "Failed to SIGKILL %{pid}: %{error}"
