en:
  content:
    exclusive: "Use exactly one of --content-stdin and --content-file."
    required: "One of --content-stdin or --content-file is required."
    stdin_help: "Read the command's entire free-form content from standard input."
    file_help: "Read the command's entire free-form content from a UTF-8 file."
    file_not_utf8: "Content file is not valid UTF-8: %{path}"
    file_read_failed: "Could not read content file %{path}: %{error}"
  agent:
    help: Manage native agent runtime state.
    conversation_help: Manage persisted native agent conversations.
  agent_conversation_reset:
    help: Reset one exact native provider session without deleting history.
    adapter_help: Native provider adapter whose exact session should be reset.
    work_kind_help: Kind of logical work owning the conversation.
    work_identity_help: Stable ticket URL, chat thread identity, or manual work identity.
  lease:
    invalid_delegation: "Nested member command rejected: execution lease delegation is invalid."
    unavailable: Person execution lease is already held.
    unavailable_with_holder: "Person execution lease is already held by pid %{pid} (%{source}: %{command})."
  sync:
    invalid_identity: "Workspace synchronization did not start because %{path} is invalid. No command changes were made; repair the file and run the command again."
  chat_updates:
    catching_up: "Received messages are waiting to be saved while workspace synchronization holds the write lock. Wait briefly and retry chat updates; do not publish or complete as blocked solely for this state."
    invalid_run: "The run has no chat input for this member. Use the current chat workflow run id."
    unavailable: "Chat reception is unavailable or its heartbeat has expired. Wait for reception to recover; do not publish."
    not_checked: "Chat updates have not been checked for this input."
    new_messages: "New source-thread messages have arrived since the last check."
    required: "%{reason} Run guildbotics member chat updates --person %{person} --run-id %{run_id}, read its result, and reconsider the operation before retrying."
